import fetcher from "./fetcher";

class RaftaServer {
    constructor() {

    }

    clientConfig() {

    }

    initializer() {
    //         تجمیع چهار سرویس اول در یک سرویس
    // init(string machineId, String sessionId, string version, string InitialDataValue, string initialPerformanceValue) => ClientConfigViewModel
    // rafta.corins.ir/api/Rafta/init?machineId=10&sessionId=20&version=3&InitialDataValue=idv&initialPerformanceValue=ipv 
    // تجمیع سرویس های : clientConfig, initializer, initialData, initialPerfotmance
    }

    sendEvent() {
        // tring EventsList
    }
}



export default RaftaServer;